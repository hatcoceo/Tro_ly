SET choice = "C"
MATCH {choice}
    CASE "A"
        PRINT "Chọn A: Thêm mới"
    CASE "B"
        PRINT "Chọn B: Sửa"
    CASE "C"
        PRINT "Chọn C: Xóa"
    CASE "D"
        PRINT "Chọn D: Thoát"
    DEFAULT
        PRINT "Lựa chọn không hợp lệ"
ENDMATCH

? nhập số điện thoại
SILENT kiểm tra số "{answer}" -> a
MATCH {a}
  CASE "True"
            PRINT "HỢP LỆ"
        CASE "False"
            PRINT "KHÔNG HỢP LỆ"
            DEFAULT 
        PRINT "Không xác định"
ENDMATCH

